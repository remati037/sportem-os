/*
 * Podrazumevani sadržaj `@modal` slota: ništa. Renderuje se na svim rutama
 * koje ne presreće `(.)tiketi/[id]`, i posle `router.back()` iz modala.
 */
export default function ModalDefault() {
  return null;
}
